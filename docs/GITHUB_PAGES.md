# GitHub Pages

Copy các file trong `web/` vào repository Pages. Không đặt secret vào frontend. GAS URL không phải password/API key.

Settings → Pages → Deploy from branch → main/root.

Frontend chỉ lưu session token trong `sessionStorage`, không lưu password.
